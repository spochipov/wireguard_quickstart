
# WireGuard VPN Server - Автоматическая установка и настройка

Комплексное решение для развертывания высокопроизводительного WireGuard VPN сервера с поддержкой IPv4/IPv6 dual-stack на Debian 12.

## 🚀 Быстрый старт

### Автоматическая установка сервера

1. **Подключитесь к серверу по SSH под root:**
```bash
ssh root@your-server-ip
```

2. **Скачайте и запустите скрипт установки:**
```bash
wget https://raw.githubusercontent.com/your-repo/wireguard/main/wg-server-setup.sh
chmod +x wg-server-setup.sh
./wg-server-setup.sh
```

3. **Сохраните информацию о сети** - скрипт выведет уникальные адресные пространства:
```
IPv4: 10.XXX.XXX.0/24
IPv6: fdXX:XXXX:XXXX::/64
```

### Добавление клиентов

После установки сервера добавьте клиентов:
```bash
wg-add-client laptop-john
wg-add-client phone-mary
wg-add-client router-office
```

## 📋 Возможности

### ✅ Автоматическая настройка сервера
- **Dual-stack поддержка**: IPv4 + IPv6 одновременно
- **Уникальные сети**: Автоматическая генерация неконфликтующих подсетей
- **Оптимизация производительности**: Настройка ядра для максимальной пропускной способности
- **Безопасность**: Автоматическая настройка брандмауэра UFW
- **Автозапуск**: Настройка systemd сервиса

### ✅ Управление клиентами
- **Простое добавление**: Одна команда для создания клиента
- **QR-коды**: Автоматическая генерация для мобильных устройств
- **Dual-stack клиенты**: IPv4 + IPv6 адреса для каждого клиента
- **Автоматическое назначение IP**: Поиск свободных адресов

### ✅ Оптимизация производительности
- **BBR congestion control**: Улучшенная производительность TCP
- **Оптимизированные буферы**: Увеличенные сетевые буферы
- **Connection tracking**: Оптимизация для большого количества соединений
- **MTU оптимизация**: Настройка для минимизации фрагментации

## 🛠 Системные требования

- **ОС**: Debian 12 (рекомендуется чистая установка)
- **Доступ**: SSH под root
- **Сеть**: Публичный IPv4 адрес
- **Ресурсы**: Минимум 512MB RAM, 1 CPU core

## 📁 Структура файлов

После установки создается следующая структура:

```
/etc/wireguard/
├── wg0.conf                    # Основной конфиг сервера
├── keys/
│   ├── server_private.key      # Приватный ключ сервера
│   └── server_public.key       # Публичный ключ сервера
└── clients/
    ├── client1.conf            # Конфиги клиентов
    ├── client2.conf
    └── ...

/usr/local/bin/
├── wg-add-client              # Скрипт добавления клиентов
├── wg-server-info             # Информация о сервере
└── wg-backup                  # Резервное копирование

/etc/sysctl.d/
└── 99-wireguard-performance.conf  # Оптимизация ядра
```

## 🔧 Управление сервером

### Основные команды

```bash
# Добавить нового клиента
wg-add-client <имя_клиента>

# Показать информацию о сервере
wg-server-info

# Показать активные подключения
wg show

# Создать резервную копию
wg-backup

# Перезапустить WireGuard
systemctl restart wg-quick@wg0

# Проверить статус
systemctl status wg-quick@wg0
```

### Просмотр логов

```bash
# Логи WireGuard
journalctl -u wg-quick@wg0 -f

# Системные логи
journalctl -f | grep wireguard
```

## 📱 Настройка клиентов

### Мобильные устройства (Android/iOS)

1. Установите приложение WireGuard
2. Отсканируйте QR-код, который показывает команда `wg-add-client`
3. Активируйте подключение

### Компьютеры (Windows/macOS/Linux)

1. Установите WireGuard клиент
2. Скопируйте содержимое файла `/etc/wireguard/clients/имя_клиента.conf`
3. Импортируйте конфигурацию в клиент

### Роутеры

Для подключения роутеров используйте тот же конфиг, но учтите:
- Настройте маршрутизацию на роутере
- Возможно потребуется настройка NAT

## 🔒 Безопасность

### Автоматические настройки безопасности

- **UFW Firewall**: Автоматическая настройка правил
- **SSH защита**: Сохранение доступа по SSH
- **Изоляция клиентов**: Клиенты не видят друг друга
- **Сильная криптография**: Современные алгоритмы WireGuard

### Рекомендации

1. **Смените SSH порт** (опционально):
```bash
# Отредактируйте /etc/ssh/sshd_config
Port 2222
# Перезапустите SSH
systemctl restart sshd
# Обновите правила UFW
ufw allow 2222/tcp
ufw delete allow 22/tcp
```

2. **Настройте автоматические обновления**:
```bash
apt install unattended-upgrades
dpkg-reconfigure unattended-upgrades
```

3. **Мониторинг подключений**:
```bash
# Добавьте в crontab для регулярной проверки
echo "*/5 * * * * root wg show > /var/log/wireguard-status.log" >> /etc/crontab
```

## 🌐 Уникальные сетевые пространства

Скрипт автоматически генерирует уникальные подсети для избежания конфликтов:

### IPv4 адресация
- **Диапазон**: 10.100-199.0-255.0/24
- **Пример**: 10.156.78.0/24
- **Сервер**: 10.156.78.1
- **Клиенты**: 10.156.78.2-254

### IPv6 адресация
- **Диапазон**: fd00::/8 (Unique Local Addresses)
- **Пример**: fd4a:b2c3:d4e5:f600::/64
- **Сервер**: fd4a:b2c3:d4e5:f600::1
- **Клиенты**: fd4a:b2c3:d4e5:f600::2-ffff

## 🔧 Устранение неполадок

### Проблемы с подключением

1. **Проверьте статус сервиса**:
```bash
systemctl status wg-quick@wg0
```

2. **Проверьте правила брандмауэра**:
```bash
ufw status verbose
```

3. **Проверьте сетевые интерфейсы**:
```bash
ip addr show wg0
```

4. **Проверьте маршрутизацию**:
```bash
ip route show table main
```

### Проблемы с производительностью

1. **Проверьте загрузку CPU**:
```bash
htop
```

2. **Мониторинг сетевого трафика**:
```bash
iftop -i wg0
```

3. **Проверьте параметры ядра**:
```bash
sysctl net.ipv4.ip_forward
sysctl net.ipv6.conf.all.forwarding
```

### Восстановление из резервной копии

```bash
# Остановите WireGuard
systemctl stop wg-quick@wg0

# Восстановите из архива
tar -xzf /root/wireguard-backups/wireguard_backup_YYYYMMDD_HHMMSS.tar.gz -C /

# Запустите WireGuard
systemctl start wg-quick@wg0
```

## 📊 Мониторинг и статистика

### Просмотр активных подключений
```bash
wg show wg0
```

### Статистика трафика
```bash
wg show wg0 transfer
```

### Информация о пирах
```bash
wg show wg0 peers
```

## 🔄 Обновления

### Обновление скриптов

```bash
# Скачайте новые версии скриптов
wget -O /usr/local/bin/wg-add-client https://raw.githubusercontent.com/your-repo/wireguard/main/wg-add-client.sh
chmod +x /usr/local/bin/wg-add-client
```

### Обновление WireGuard

```bash
apt update && apt upgrade wireguard wireguard-tools
systemctl restart wg-quick@wg0
```

## 📞 Поддержка

При возникновении проблем:

1. Проверьте логи: `journalctl -u wg-quick@wg0`
2. Убедитесь, что все порты открыты
3. Проверьте конфигурацию: `wg-quick strip wg0`
4. Создайте issue в репозитории с подробным описанием проблемы

## 📄 Лицензия

MIT License - используйте свободно для личных и коммерческих целей.
